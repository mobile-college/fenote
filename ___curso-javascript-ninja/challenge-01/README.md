# Desafio Semana #1

```js
// Declarar uma vari谩vel chamada `myvar`, sem valor.
?

// Ap贸s declarada, atribua o valor 10 脿 vari谩vel `myvar`.
?

// Declare uma nova vari谩vel chamada `soma`, e adicione uma instru莽茫o somando os valores 15 e 8.
?

// Atribua 脿 vari谩vel `soma` todo o valor dela, somando 1, usando o operador de soma abreviado.
?

// Atribua 脿 vari谩vel `soma` todo o valor dela, multiplicando por 3, usando o operador de multiplica莽茫o abreviado.
?

// Qual 茅 o valor da vari谩vel `soma` at茅 aqui?
?

// Declare uma vari谩vel chamada `souninja`, atribuindo 脿 ela o valor booleano que representa `verdadeiro`.
?

// Declare uma vari谩vel chamada `comida` que recebe um array com os valores 'arroz', 'feij茫o' e 'ovo'.
?

// Digite a instru莽茫o que imprime o valor de 'feijao', que est谩 na vari谩vel `comida`.
?

// Digite o c贸digo que verifica se a vari谩vel `soma' 茅 igual a vari谩vel `myvar` (testando tamb茅m o tipo).
?

// Digite o c贸digo que verifica se a vari谩vel `myvar` 茅 menor ou igual 脿 vari谩vel `soma`.
?

// Crie uma fun莽茫o chamada `divisao` que receba como par芒metro dois n煤meros, e retorne o resultado da divis茫o entre eles.
?

// Invoque a fun莽茫o criada acima, passando os par芒metros 10 e 2.
?
```